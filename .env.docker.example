# =================================================
#       KONFIGURASI UNTUK DATABASE CONTAINER
# =================================================
# Variabel ini digunakan oleh service 'db' di docker-compose.yml
# untuk membuat database dan user saat pertama kali dijalankan.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mysecretpassword # Anda bisa ganti ini jika mau
POSTGRES_DB=mentora_db

# =================================================
#       KONFIGURASI UNTUK APLIKASI BACKEND
# =================================================

# --- Koneksi Database ---
# Menggunakan 'db' sebagai nama host karena itu adalah nama service
# database di dalam jaringan Docker.
DATABASE_URL="postgresql://postgres:mysecretpassword@db:5432/mentora_db"

# --- Konfigurasi JWT ---
SECRET_KEY="ganti-dengan-kunci-rahasia-yang-sangat-aman-dan-panjang"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- Kunci API & Layanan Eksternal (WAJIB DIISI) ---
GEMINI_API_KEY="MASUKKAN_KUNCI_API_GEMINI_ANDA_DI_SINI"
GCS_BUCKET_NAME="nama-bucket-google-cloud-storage-anda"

# --- Path Kredensial Google Cloud ---
# Path ini mengarah ke LOKASI FILE DI DALAM CONTAINER DOCKER,
# bukan di komputer Anda. Ganti 'gcp-creds.json' dengan nama file Anda.
GOOGLE_APPLICATION_CREDENTIALS="/app/credentials/gcp-creds.json"